const std = @import("std");

pub fn main() !void {
    const data = [_]u32{ 1, 2, 3, 4 };
    var i: usize = 0;
    _ = i;
    var escape_count: usize = 0;
    _ = escape_count;
    while (i < src.len) {

    }
}
